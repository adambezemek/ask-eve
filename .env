# Local dev only. On Netlify, set environment variables in the UI:
#  - OPENAI_API_KEY
#  - DATABASE_URL (Netlify DB will set this when linked)
OPENAI_API_KEY=sk-REPLACE_ME
DATABASE_URL=REPLACE_WITH_NETLIFY_DB_URL_WHEN_TESTING_LOCALLY
