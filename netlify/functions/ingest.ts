// TODO: Paste ingest code from the "Ingest Content & Generate Embeddings" section of the guide.
// Should: chunk your docs, call OpenAI embeddings, insert into Netlify DB via Drizzle.
export default async () => {
  return new Response("Ingest endpoint not yet implemented", { status: 501 });
};
